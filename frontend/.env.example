# 复制为 .env.local（本地）或在 Vercel 项目设置中配置
# 本地开发：留空，前端会用 /api 经 next.config.js 转发到 127.0.0.1:8000
# 生产环境（Vercel）：填写 Render 后端 URL，例如：
# NEXT_PUBLIC_API_BASE=https://ai-research-agent-api.onrender.com
NEXT_PUBLIC_API_BASE=
